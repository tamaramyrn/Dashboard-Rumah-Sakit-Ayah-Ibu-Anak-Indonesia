-- ============================================================
--  ERD Basis Data Operasional (OLTP) — SIMRS Rawat Inap BPJS
--  RSU Ayah Ibu Anak Indonesia
--
--  FK ditulis INLINE pada kolom (REFERENCES ...) — supaya di draw.io
--  muncul sebagai kolom biasa, BUKAN baris "FOREIGN KEY ...".
--
--  Import: draw.io  →  + (Insert)  →  Advanced  →  "SQL..."  →  paste  →  Insert
--  (Kalau garis relasi tetap tidak muncul, pakai file erd_operasional.drawio.)
-- ============================================================

-- ============ TABEL MASTER (12) ============

bi